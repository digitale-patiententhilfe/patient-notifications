// Test setup file
// For component tests with React, you would import @testing-library/jest-dom and cleanup
// Since we're using node environment for service/util tests, we keep this minimal

import { afterEach } from "vitest";

afterEach(() => {
  // Cleanup logic here if needed
});
